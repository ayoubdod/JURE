# Vite production defaults (baked in at `vite build` if Railway vars are unset).
# Prefer setting the same keys in the Railway service Variables panel so builds
# stay correct without relying on this file being present in git.
VITE_API_URL=https://jure-production-a298.up.railway.app
# Juria is compiled out unless this is exactly "true" at `vite build` time.
VITE_JURIA_ENABLED=true
# Optional override; omit to auto-derive wss://jure-production-a298.up.railway.app
# VITE_WS_BASE=wss://jure-production-a298.up.railway.app
